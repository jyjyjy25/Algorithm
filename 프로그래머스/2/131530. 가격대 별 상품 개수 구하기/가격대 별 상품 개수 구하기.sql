# -- 코드를 입력하세요
# WITH TLB AS (
#     SELECT CASE
#             WHEN LEFT(PRICE / 10000, 1) = 0 THEN "0"
#             WHEN LEFT(PRICE / 10000, 1) = 1 THEN "1"
#             WHEN LEFT(PRICE / 10000, 1) = 2 THEN "2"
#             WHEN LEFT(PRICE / 10000, 1) = 3 THEN "3"
#            END AS PRICE_GROUP
#     FROM PRODUCT
# )
# SELECT TRUNCATE(PRICE / 10000, 0) * 10000 AS PRICE_GROUP,
#        COUNT(PRODUCT_ID) AS PRODUCTS
# FROM PRODUCT
# GROUP BY PRICE_GROUP
# ORDER BY PRICE_GROUP

SELECT TRUNCATE(PRICE / 10000, 0) * 10000, COUNT(*)
FROM PRODUCT
GROUP BY TRUNCATE(PRICE / 10000, 0) * 10000
ORDER BY 1

# SELECT  AS PRODUCTS
# FROM TLB
# GROUP BY 1
# ORDER BY 1